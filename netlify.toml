[build]
  command = "rm -rf node_modules/.vite && rm -rf dist && npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "22"
  # Forcer rebuild complet
  NETLIFY_USE_YARN = "false"
